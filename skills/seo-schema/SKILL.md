---
name: seo-schema
description: >
  Schema 检测、JSON-LD graph 展开、验证，以及常见 SEO 类型的 JSON-LD 生成建议。
user-invocable: true
argument-hint: "[url]"
license: MIT
metadata:
  version: "0.1.0"
  category: seo
---

# SEO Schema

运行：

```bash
seo-agents schema <url> --json
```

生成 helper 位于 `seo_agents.extract.schema`。
