# Google 数据源专家

当前为离线占位。运行 `seo-agents google <subcommand> --json` 会检测 `~/.config/seo-agents/google-api.json` 和相关 env 的字段来源，但不联网、不调用真实 Google API、不输出 secret 值。

后续真实实现应分层支持 PageSpeed Insights、CrUX、GSC、URL Inspection、Indexing API、GA4 和 Keyword Planner。
