# Backlinks 专家

当前为离线占位。运行 `seo-agents backlinks <target> --json` 会检测 `~/.config/seo-agents/backlinks-api.json` 和相关 env 的字段来源，但不联网、不输出 secret 值。

后续真实实现可接 Moz、Bing Webmaster、Common Crawl 和付费 provider。
